How To Run:

1.) To run the program open interactive sml in terminal.
2.) Enter: `use "loader.sml";`
3.) Run printResults("<file_name>");
4.) The program will then output the lex result, followed by parse tree in postorder(in a list format).
5.) In case of any parsing exception the handler will catch it and display apt. results.